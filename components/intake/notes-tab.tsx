import { Card } from "@/components/ui";

export function NotesTab() {
  return (
    <div className="p-8">
      <Card title="Care Coordination Notes">
        <p className="text-sm text-slate-500">
          This tab is waiting on the Care Coordination Notes form template — send it over and this section will
          get built out to match, same as Demographics.
        </p>
      </Card>
    </div>
  );
}
